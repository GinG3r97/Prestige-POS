<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for Supabase (HTTPS) and LAN thermal printing (raw TCP to
         port 9100). Without this, a RELEASE build has no network at all —
         the debug manifest grants it for dev only, so it must be declared
         here too. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Lets the app check it's on Wi-Fi before reaching a LAN printer so we
         can show a clearer "not on the same network" message. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Owner sets the store's attendance geofence from the POS tablet (at the
         store), so we capture the device location once on demand. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Bluetooth thermal printing (PT-210 / Goojprt / generic ESC-POS).
         We connect to PAIRED (bonded) printers only — never active discovery —
         so NO location permission is needed on any Android version.
         · Android ≤ 11 (API ≤ 30): legacy install-time BLUETOOTH + ADMIN. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!--   · Android 12+ (API 31+): runtime BLUETOOTH_SCAN + BLUETOOTH_CONNECT.
           neverForLocation asserts we don't derive physical location from BT,
           so the OS skips the location-permission prompt entirely. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Mark BT as optional so tablets without a BT radio (Wi-Fi-only LAN
         printing) can still install the app. -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <!-- Tablet POS: large-screen, landscape app. Declaring screen support
         keeps it off the Play Store "phone-only" filter and marks it as
         designed for tablets. -->
    <supports-screens
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:requiresSmallestWidthDp="600"/>

    <application
        android:label="Prestige POS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
